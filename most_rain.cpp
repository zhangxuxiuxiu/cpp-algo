#include <iostream>
#include <vector>

using namespace std;

// left to right, one ge bucket, fill it
// right to left, one gt bucket, fill it
int most_rain(vector<int> const& buckets){

}

int main(){
	cout << most_rain({4,2,3,3,4,5,5,6,5,7}) << '\n';

	return 0;
}
