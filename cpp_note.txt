对象：
	1) 虚表
		每个有虚函数的类，都有一个虚表，虚表中每4个byte表示一个虚函数的地址，每个类示例开始的位置会存储一个指向虚表的地址，从而方便调用虚表的函数。
		有虚函数或者虚继承的类，析构函数一般为虚函数，从而在析构时动态释放资源；
		构造函数不能为虚函数，在构造父类成员时，子类尚不存在。在构造函数中调用虚函数时，只会调用本函数的实现。
		虚函数是跟类对象动态绑定的，所以不能是静态函数。

	2) 虚继承的实现
		虚基类总是先于非虚基类构造，与它们在集成体系中的次序和位置无关。如果有多个虚基类，则按它们在派生列表中出现的顺序从左到右依次构造。

